/**
 * Tipos de contacto para un usuario.
 */

export enum TContact {
    EMAIL = 'email',
    PHONE = 'phone',
    WHATSAPP = 'whatsapp',
}

/**
 * Tipos de suscripción disponibles.
 */
export enum TSubscription {
    STARTER = 'starter',
    PRO = 'pro',
    ENTERPRISE = 'enterprise',
}